using Newtonsoft.Json;
using KDESessionManager.Objects;

namespace KDESessionManager.SessionHandling
{
    public class ProcessSessionData
    {
        public static void ProcessSingleSession(string windowFilterPath, string sessionPath, string outputPath)
        {
            
        }

        public static void ProcessMultipleSessions(string windowFilterPath, string sessionListPath, bool mergeSessions, string outputPath)
        {
            
        }
    }
}